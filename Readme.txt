use webapp.py file to run the web application.
Use command prompt:
>streamlit run webapp.py
1. make sure the command line is pointing at the current directory(directory in which webapp.py is present.)
2. streamlit must be installed in your systems.